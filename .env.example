# Required to enable the "Ask about Mindy" chat widget (app/api/chat).
# Get a key at https://console.anthropic.com/ and set it in Vercel Project Settings
# → Environment Variables (never commit the real key).
ANTHROPIC_API_KEY=
